// PulseApp - Конфигурация
// Измени API_BASE_URL если бэкенд на другом адресе
// Пустая строка '' означает что API на том же домене (pulse.lvakarin.ru/api)

const APP_CONFIG = {
    // URL бэкенда (без trailing slash)
    // Примеры:
    //   ''                           — тот же домен (рекомендуется для прода)
    //   'http://localhost:5000'      — локальная разработка
    //   'https://pulse.lvakarin.ru'  — прямой адрес
    API_BASE_URL: '',
};
